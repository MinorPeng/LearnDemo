<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a14512.ipcdemo">

    <application
        android:name=".more_process.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".more_process.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".more_process.SecondActivity"
            android:configChanges="screenLayout"/>
            <!--android:process=":remote"-->
        <activity android:name=".more_process.ThirdActivity"
            android:configChanges="screenLayout"/>
            <!--android:process="com.ryg.chapter_2.remote"-->
    </application>

</manifest>